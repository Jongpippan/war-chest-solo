{
  "name": "war-chest-solo-local",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "serve": "python3 -m http.server 4173",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
