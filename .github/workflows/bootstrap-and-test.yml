name: Bootstrap and test

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply staged v0.8 board and UX patch
        if: ${{ hashFiles('.v08/apply.py') != '' }}
        run: python .v08/apply.py
      - name: Install dependencies
        run: npm install --no-package-lock
      - name: Run tests
        run: npm test
      - name: Run bot simulation
        run: node test/simulate.mjs
      - name: Install Playwright for one-time visual verification
        if: ${{ hashFiles('.v08/visual.mjs') != '' }}
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install chromium
      - name: Visual verification at desktop viewport sizes
        if: ${{ hashFiles('.v08/visual.mjs') != '' }}
        run: |
          python3 -m http.server 4173 > /tmp/war-chest-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID || true' EXIT
          sleep 1
          node .v08/visual.mjs
      - name: Upload visual verification screenshots
        if: ${{ hashFiles('.v08/screens') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: v08-visual-verification
          path: .v08/screens/*.png
      - name: Commit v0.8 source and build output
        if: ${{ hashFiles('.v08/apply.py') != '' }}
        run: |
          rm -rf .v08
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat: accurate 2-player board and polished table UX v0.8'
          git push
